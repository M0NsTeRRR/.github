# THIS FILE IS GENERATED! DO NOT EDIT! Maintained by Pulumi
name: create pull request for automation-sync
on:
  push:
    branches:
      - {{ branch_name }}
permissions:
  contents: read
  pull-requests: write
  checks: write
jobs:
  create-pull-request:
    name: Create Pull Request
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Create Pull Request
        run: "gh pr view {{ branch_name }} && gh pr reopen {{ branch_name }} || gh pr create -B {{ default_branch_name }} -H {{ branch_name }} --title 'chore(pulumi): auto-applied' --body 'This PR is auto-generated by https://github.com/M0NsTeRRR/.github.'"
        env:
          GITHUB_TOKEN: {{ "${{" }} secrets.GITHUB_TOKEN {{ "}}" }}